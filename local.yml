---
- hosts: localhost
  become: true
  collections:
    - community.general
  roles:
    - { role: base, tags: ["base"] }
    - { role: xorg, tags: ["xorg"] }
    - { role: bspwm, tags: ["bspwm"], become_user: "{{ username }}" }
  vars_prompt:
    - name: username
      prompt: "Enter username: "
      private: no
    - name: password
      prompt: "Enter password: "
      unsafe: yes
      private: yes
